// API Client Export

import { mockClient } from './mockClient';
// import { httpClient } from './httpClient';

// Switch between mock and HTTP client here
export const apiClient = mockClient;
// export const apiClient = httpClient; // Uncomment when backend is ready
